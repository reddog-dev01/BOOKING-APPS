# Server-side Google Places key shared with the API (billing enabled). Replace via README step 5.
PLACES_API_KEY="__REPLACE_WITH_SERVER_PLACES_API_KEY__"
# Browser referer used when applying HTTP restrictions to the Maps JavaScript key.
# Update the port if `pnpm --filter web dev` falls back to 3000 or 3008.
GOOGLE_MAPS_REFERER=http://localhost:3005/
# Public-facing API origins consumed by the Next.js app
NEXT_PUBLIC_API_BASE=http://127.0.0.1:3006
INTERNAL_API_BASE=http://127.0.0.1:3006
# Browser Google Maps JavaScript API key (HTTP referrer restricted). Replace via README step 5.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="__REPLACE_WITH_BROWSER_MAPS_API_KEY__"
# Shared customer journeys
NEXT_PUBLIC_QUOTE_PATH=/pricing/quote
NEXT_PUBLIC_BOOKINGS_PATH=/bookings
